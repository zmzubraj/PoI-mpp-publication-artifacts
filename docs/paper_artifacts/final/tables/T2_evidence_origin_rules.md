| Origin | Canonical meaning | Lifecycle ceiling or rule | Current final-paper use | Boundary note |
|---|---|---|---|---|
| `REAL_MODEL_EXECUTION` | A run produced by the requested real model under the canonical publication boundary. | May be interpreted only at its recorded disposition and scope. | Canonical E1 and E2 artifacts are `INCONCLUSIVE`; externally attested E3 is `NOT_SUPPORTED` because FAR 0.500 (1/2) exceeded `alpha_sem = 0.25` (FRR 0.167 (1/6), ABSTAIN 0.125 (1/8), coverage 0.875 (7/8), Brier 0.178; n=8, invalid n=2). | Real execution does not clear the freeze gate or independently support a claim beyond the manifest's method ceiling. |
| `FOUNDRY_MEASUREMENT` | A local EVM measurement surface generated from the live Solidity or Foundry boundary. | May support publication claims within its measured local scope. | Canonical E7/T12/F12 is `SUPPORTED` for local boundedness. | Local Anvil or Foundry measurements must not be relabeled as Ethereum mainnet production costs. |
| `REPRODUCIBLE_SIMULATION` | A deterministic, replayable simulation surface with frozen inputs and contract-bound interpretation. | May support simulation-bounded claims when the configured artifacts exist. | Canonical E4 and E8 are `INCONCLUSIVE`; E5 and E6 are `SUPPORTED` only within declared simulation scenarios. | Simulation evidence cannot be upgraded into real deployment proof or stronger-than-supported language. |
| `SYNTHETIC_NON_EVIDENCE` | A plumbing, fixture, or non-evidentiary development surface used to validate mechanics. | May reach `SEMANTICALLY_VALID` but never `FROZEN` or `PUBLICATION_ELIGIBLE`. | Not admissible as publication evidence. | Synthetic mechanics and fixtures must never satisfy the freeze gate or support a scientific finding. |
