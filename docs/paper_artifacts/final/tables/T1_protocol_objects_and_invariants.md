| Layer | Core object or invariant | Evidentiary status | Current paper use |
|---|---|---|---|
| Policy | `C_P` binds model rules, task families, audit compiler, semantic verifiers, DA, disputes, economics, and upgrades. | Conceptual architecture | Protocol definition table |
| Task | `C_T` binds one prompt or input root, one model, one task class, one budget, one deadline, and one nonce. | Conceptual architecture | Protocol definition table |
| Model/Runtime | `C_M` binds the exact open-weight or confidential-model runtime surface, tokenizer, precision, and sharding or MoE policy. | Conceptual architecture | Protocol definition table |
| Execution | One native inference produces the response and the committed execution root `R_X`. | Conceptual architecture | Protocol definition table |
| Intelligence | IEC root `R_E` binds claims, evidence, dependencies, constraints, and uncertainty tags. | Conceptual architecture | Protocol definition table |
| Audit | Post-commit `eta` selects `S_X` and `S_S` after commitment finality; no second frontier-model inference is required. | Conceptual architecture | Protocol definition table |
| Dispute | A permissionless optimistic challenge path arbitrates invalid execution or protocol faults. | Conceptual architecture | Protocol definition table |
| Receipt/DA | Artifacts must remain retrievable through the challenge and retention horizon before authority can mature. | Conceptual architecture | Protocol definition table |
| Resource | Matured receipts create bounded epoch-local credit and bounded next-epoch weight. | Conceptual architecture | Protocol definition table |
| Consensus | PoI weight is consumed by a validator or sequencer selection layer rather than by heavy AI execution inside the EVM. | Conceptual architecture | Protocol definition table |
