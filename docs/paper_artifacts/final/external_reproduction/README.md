# External reproduction handoff

Status: `WAITING_EXTERNAL_REPRODUCTION`

This directory packages deterministic inputs and exact return contracts for a
real external team. It does not assert that anyone outside the producing team
has executed, reproduced, reviewed, signed, or approved the PoI MPP.

Required external return:

1. completed clean-room protocol and raw command log;
2. canonical verifier outputs and output hashes;
3. completed discrepancy report;
4. reproduction attestation naming accountable individuals and their
   independence basis;
5. detached OpenSSH signature over the exact attestation;
6. external allowed-signers file held outside the repository.

The package remains `WAITING_EXTERNAL_REPRODUCTION` until every return is
authenticated and reconciled. Agreement with repository outputs is not by
itself scientific validity, novelty closure, or publication readiness.
